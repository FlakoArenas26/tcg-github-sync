
APITCG_API_KEY=tu_api_key_de_apitcg
# Opcional para uso local: si ya tenes `gh` autenticado o credenciales git
# configuradas, dejalo vacio y el push usa esas. Solo hace falta llenarlo
# en entornos sin git/gh configurado (ej. CI).
GITHUB_TOKEN=
GITHUB_REPO=TU_USUARIO/TU_REPO
REQUESTS_PER_MINUTE=30
